# ControlLight Python dependencies
#
# Recommended install order:
#   python -m pip install -e diffusers
#   python -m pip install -r requirements.txt
#   python -m pip install -e .
#
# Notes:
# - This file is intended to match the dependency set in pyproject.toml.
# - Prediction and training are expected to run in the same environment
#   (the project defaults to the `aitoolkit` conda environment through
#   scripts/project_env.sh).
# - The versions below are pinned to the clean pip-installable stack that was
#   validated for standalone installation, inference, and training startup.
# - The older private `design_materials_20260522_scene_dark_aligned_v3`
#   reference came from an internally drifted environment that is not
#   reproducible via a standard public pip resolve.

# Core
setuptools==69.5.1
torch==2.9.1
torchvision==0.24.1
torchaudio==2.9.1
transformers==5.5.3
accelerate==1.13.0
peft==0.19.1
safetensors==0.7.0
numpy==1.26.4
einops==0.8.2
huggingface_hub==1.11.0
sentencepiece==0.2.1
pyyaml==6.0.3
python-dotenv==1.2.1

# Training / optimization
bitsandbytes==0.49.2
tensorboard==2.20.0
kornia==0.8.2
torchao==0.10.0
k-diffusion==0.1.1.post1
lycoris-lora==1.8.3
timm==1.0.22
open-clip-torch==3.3.0
optimum-quanto==0.2.4
lpips==0.1.4

# Image / data processing
Pillow==11.3.0
albumentations==1.4.15
albucore==0.0.16
opencv-python==4.11.0.86
av==17.0.1
invisible-watermark

# Config / utilities
omegaconf==2.3.0
pydantic==2.13.3
toml==0.10.2
oyaml==1.0

# Scientific / visualization
scipy==1.12.0
matplotlib==3.10.1
